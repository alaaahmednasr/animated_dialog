name: cool_animated_dialog
description: "Animated Dialog is package that provides seamless solutions for alerts and confirmation dialogs with appealing animations"
version: 0.0.3
homepage: https://github.com/alaaahmednasr/animated_dialog.git

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  lottie: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

